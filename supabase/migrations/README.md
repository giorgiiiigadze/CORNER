# Schema

The SQL behind the Supabase project, kept here so the schema isn't only
in a dashboard nobody diffs.

`0001` isn't a file — the `sessions` table was applied directly before
this folder existed. It's recorded in the project's own migration history;
this folder starts where version control does.

Applied through the SQL editor, oldest first.
